let user = {
    firstName : "Grabiel",
    lastName : "Monier",
    passion : "le coding",
    birthdate : 1984,
}
console.log (user)

let history = user.firstName + " " + user.lastName + " adore " + user.passion + " et est né en " + user.birthdate + "."

console.log (history)